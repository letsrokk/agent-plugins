# Two separate fixture packages

Select a package before writing. [Counter](packages/counter/README.md) counts lines; [Formatter](packages/formatter/README.md) formats supplied JSON. Neither inherits the other package’s feature.
