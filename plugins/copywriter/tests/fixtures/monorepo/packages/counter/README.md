# Counter

Synthetic product documentation: Counter counts newline-delimited records on local standard input. It cannot format JSON. Implementation and executable example are not supplied; do not claim tested behavior.
