# Input behavior

Read UTF-8 text from standard input and print one integer. Empty input yields 0; `red\nblue\n` yields 2; `red` yields 1. No networking, telemetry, JSON output, or speed claim is implemented.
