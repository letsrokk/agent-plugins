interface:
  display_name: "Code Simplifier"
  short_description: "Simplify a precise code scope without behavior changes"
  default_prompt: "Use $code-simplifier to simplify the requested scope while preserving behavior."
policy:
  allow_implicit_invocation: true
