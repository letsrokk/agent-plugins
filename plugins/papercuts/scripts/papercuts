#!/usr/bin/env node
import {main} from '../src/cli.js';
if (Number(process.versions.node.split('.')[0]) < 24) {
  console.error('Papercuts requires Node.js 24 or later on PATH.');
  process.exitCode = 78;
} else process.exitCode = main();
