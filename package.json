{
  "name": "agent-plugins",
  "private": true,
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "test": "node --test tests/*.test.js",
    "validate": "node scripts/validate_marketplaces.js"
  },
  "devDependencies": {
    "smol-toml": "1.8.0"
  }
}
